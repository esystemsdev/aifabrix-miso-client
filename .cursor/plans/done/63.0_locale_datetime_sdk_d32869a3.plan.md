---
name: 63.0 locale datetime sdk
overview: Add a standardized browser-locale datetime formatting utility to @aifabrix/miso-client for reuse across consumer UI apps (miso, dataplane), with consistent API, exports, tests, and documentation.
todos:
  - id: scope-and-prerequisites
    content: Confirm scope, applicable rules, and datetime API contract/non-goals before implementation
    status: completed
  - id: implement-datetime-types-and-utils
    content: Implement datetime types and Intl.DateTimeFormat-based utilities with browser-locale/browser-timezone defaults
    status: completed
  - id: export-sdk-surface
    content: Expose datetime utilities and types via sdk-exports and package public surface
    status: completed
  - id: implement-automated-tests
    content: Implement all automated tests listed in `## Expected Automated Tests` and verify they pass before final validation gates
    status: completed
  - id: update-public-docs
    content: Document datetime utility usage and add docs index + README references
    status: completed
  - id: create-temp-consumer-agent-handoff
    content: Create a temporary numbered handoff document in project .temp for consumer-project AI agents (miso/dataplane) with adoption instructions
    status: completed
  - id: run-ordered-validation-gates
    content: Run mandatory silent validation gates in required order and inspect .temp/validation logs on silent failures
    status: completed
  - id: final-dod-closure
    content: Verify Definition of Done, readiness statements, and plan validation report completion
    status: completed
isProject: false
---

# 63.0 Locale Datetime Standardization in miso-client

## Goal
Introduce a single, reusable datetime formatting surface in `@aifabrix/miso-client` so consumer UIs render date/time consistently while respecting each user's browser locale and local timezone.

## Scope
### In scope
- Add framework-agnostic datetime utilities and related types in `miso-client`.
- Export datetime API from SDK public surface for consumer UI apps.
- Add automated unit tests for locale/timezone/fallback behavior.
- Update public SDK docs and README usage for datetime formatting.
- Create temporary rollout handoff document under project `.temp/` for consumer-project AI agents.

### Out of scope
- Refactoring consumer repositories (`aifabrix-miso`, `aifabrix-dataplane`) in this plan.
- Adding framework-specific wrappers (React/Vue hooks or components).
- Adding relative-time/humanized duration features (for example `2m ago`).
- Introducing third-party date libraries (`moment`, `dayjs`, `date-fns`).

## Current Evidence
- SDK currently exposes reusable utility modules through [`/home/dev02/aifabrix-miso-client/src/sdk-exports.ts`](/home/dev02/aifabrix-miso-client/src/sdk-exports.ts), but has no dedicated datetime formatter export.
- Consumer UIs currently format datetimes differently (example patterns found in [`/home/dev02/aifabrix-miso/packages/miso-ui/src/app/components/logs/logUtils.ts`](/home/dev02/aifabrix-miso/packages/miso-ui/src/app/components/logs/logUtils.ts) and [`/home/dev02/aifabrix-dataplane/app-ui/utils/dateTime.ts`](/home/dev02/aifabrix-dataplane/app-ui/utils/dateTime.ts)).

## Rules and Standards
- [`/home/dev02/aifabrix-miso-client/.cursor/rules/project-rules.mdc`](/home/dev02/aifabrix-miso-client/.cursor/rules/project-rules.mdc) - **TypeScript conventions and public API naming**: keep outputs camelCase and keep utilities framework-agnostic.
- [`/home/dev02/aifabrix-miso-client/.cursor/rules/project-rules.mdc`](/home/dev02/aifabrix-miso-client/.cursor/rules/project-rules.mdc) - **Testing conventions**: add Jest unit coverage for success and error/edge paths.
- [`/home/dev02/aifabrix-miso-client/.cursor/rules/project-rules.mdc`](/home/dev02/aifabrix-miso-client/.cursor/rules/project-rules.mdc) - **Security guidelines**: avoid exposing secrets, keep behavior safe/no-throw on malformed inputs, and document consumer-safe usage.
- [`/home/dev02/aifabrix-miso-client/.cursor/rules/project-rules.mdc`](/home/dev02/aifabrix-miso-client/.cursor/rules/project-rules.mdc) - **When adding new features**: update types, exports, tests, and docs as one complete change set.

Key requirements applied:
- All public API outputs and option fields remain camelCase.
- Add or update tests for both normal and invalid/edge input behavior.
- Update documentation because this changes public SDK usage surface.

## Before Development
- [x] Confirm final API contract and fallback semantics (`formatDateTime`/`formatDate`/`formatTime`/`createDateTimeFormatter`).
- [x] Confirm non-goals to prevent scope creep during implementation.
- [x] Re-check existing utility/test patterns in `src/utils/*.ts` and `tests/unit/*.test.ts` for naming and style alignment.

## Implementation Plan (miso-client)
1. Define contract and non-goals before coding:
   - Freeze supported input types (`string | number | Date | null | undefined`), no-throw policy, and invalid-value semantics.
   - Keep defaults on browser/runtime locale and browser local timezone.

2. Add a new public datetime utility module in the SDK:
   - Create [`/home/dev02/aifabrix-miso-client/src/utils/datetime.utils.ts`](/home/dev02/aifabrix-miso-client/src/utils/datetime.utils.ts).
   - Implement locale-aware formatting using `Intl.DateTimeFormat` with defaults:
     - locale: browser/runtime locale (`undefined` locale argument)
     - timezone: browser local timezone (no forced UTC)
   - Provide stable, reusable API with safe fallbacks for empty/invalid input.

3. Add SDK datetime types:
   - Create [`/home/dev02/aifabrix-miso-client/src/types/datetime.types.ts`](/home/dev02/aifabrix-miso-client/src/types/datetime.types.ts).
   - Define options/presets (for example: display variant, seconds toggle, explicit locale/timezone override, fallback placeholder).

4. Export datetime API from package entry points:
   - Update [`/home/dev02/aifabrix-miso-client/src/sdk-exports.ts`](/home/dev02/aifabrix-miso-client/src/sdk-exports.ts) to export new types and formatter functions.
   - Keep top-level export behavior via [`/home/dev02/aifabrix-miso-client/src/index.ts`](/home/dev02/aifabrix-miso-client/src/index.ts) unchanged (`export * from "./sdk-exports"`).

5. Implement automated tests for deterministic behavior and edge cases:
   - Create [`/home/dev02/aifabrix-miso-client/tests/unit/datetime.utils.test.ts`](/home/dev02/aifabrix-miso-client/tests/unit/datetime.utils.test.ts).
   - Cover:
     - valid ISO input formatting
     - empty/placeholder handling
     - invalid timestamp fallback behavior
     - locale differences (by explicit locale override in tests)
     - timezone override behavior (while default remains browser local)

6. Document consumer usage and migration path:
   - Add a focused doc page: [`/home/dev02/aifabrix-miso-client/docs/datetime-formatting.md`](/home/dev02/aifabrix-miso-client/docs/datetime-formatting.md).
   - Update docs index in [`/home/dev02/aifabrix-miso-client/docs/README.md`](/home/dev02/aifabrix-miso-client/docs/README.md).
   - Add short README usage section in [`/home/dev02/aifabrix-miso-client/README.md`](/home/dev02/aifabrix-miso-client/README.md) with import + examples for UI apps.

7. Create a temporary consumer-agent handoff document for rollout:
   - Create a markdown document under project-root `.temp/` (for example `.temp/agent-handover/`), not in any external temp path.
   - Use the next sequential numeric filename prefix in that folder (for example `01-...`, `02-...`) when creating the file.
   - Include instructions targeted at AI agents working in consumer repos (`aifabrix-miso` and `aifabrix-dataplane`): what to import from SDK, how to replace local datetime helpers, fallback behavior expectations, and verification checklist.

## Proposed Public API Shape
- `formatDateTime(value, options?)` — main formatter for UI display.
- `formatDate(value, options?)` and `formatTime(value, options?)` — optional helpers for common UI cases.
- `createDateTimeFormatter(options?)` — optional factory for repeated rendering paths (tables/lists) to avoid repeated option assembly.

All APIs remain framework-agnostic (no React dependency in SDK).

## Recommended Contract Decisions
- **Input types**: support `string | number | Date | null | undefined` and normalize internally before formatting.
- **No-throw behavior**: formatter functions should never throw on bad input; return `fallback` (default `-`) or original trimmed value per documented rule.
- **Default rendering policy**: when options are omitted, use browser/runtime locale and browser local timezone.
- **Override policy**: allow explicit `locale` and `timeZone` only as opt-in overrides; do not force global locale/timezone state.
- **Invalid-value semantics**: define one consistent behavior for invalid/parsing-failed inputs and keep it identical across `formatDateTime`, `formatDate`, `formatTime`.
- **Deterministic factory behavior**: `createDateTimeFormatter(options?)` must apply the same formatting semantics as one-off formatter calls.

## Recommended Best Practices for Execution
- **Prefer pure, side-effect-free formatters**: keep formatting functions deterministic and independent from global mutable state so they are safe in React render paths and reusable in any UI layer.
- **Centralize parse + guard behavior**: handle `null`/`undefined`/empty strings/placeholders and invalid dates in one utility path to avoid each consumer re-implementing fallback logic differently.
- **Use `Intl.DateTimeFormat` instance reuse for hot paths**: when rendering large tables, rely on `createDateTimeFormatter()` (or equivalent memoized factory) to avoid creating a new formatter per cell.
- **Design options as additive and backward-compatible**: keep defaults stable (browser locale + browser timezone), and only add optional overrides (`locale`, `timeZone`, `dateStyle`, `timeStyle`) to minimize breaking changes for consumers.
- **Test with explicit locale/timezone inputs**: avoid brittle tests tied to CI machine locale; pin locale/timezone in test cases that verify output shape and reserve separate tests for default-runtime behavior.
- **Document UX semantics explicitly**: describe what happens for invalid or missing values (for example fallback `-`) and recommend consumer usage patterns for list rows, details pages, and audit/log timestamps.
- **Include migration examples from existing UI patterns**: in docs, show before/after replacement snippets for `miso-ui` and `dataplane` style usages to speed up adoption and reduce duplicate date helpers.

## Risks and Mitigations
- **Locale output variance across environments**: mitigate with tests that pin explicit locale/timezone and assert stable fragments/shape.
- **Runtime differences (browser vs Node/Jest ICU data)**: mitigate by avoiding exact full-string assertions for default locale tests and adding explicit-locale tests.
- **Inconsistent consumer fallback behavior after rollout**: mitigate with a required migration checklist in the temporary handoff document.

## Expected Automated Tests
- Add `tests/unit/datetime.utils.test.ts` for datetime utility contract coverage.
- Validate explicit locale formatting cases (for example `pt-PT`, `fi-FI`) with controlled timezone overrides.
- Validate default-runtime behavior without brittle full-string assertions.
- Validate empty input, placeholder input, and invalid datetime fallback behavior.
- Validate `createDateTimeFormatter(options?)` parity with one-off formatter behavior.

## Validation
Run gates in this exact order:

```bash
pnpm run tests:typecheck:silent
pnpm run build:silent
pnpm run fmt:silent
pnpm run md:lint:silent
# optional only if md:lint reports fixable issues
pnpm run md:fix:silent
pnpm run lint:silent
pnpm run test:silent
```

Silent failure handling:
- On any `*:silent` failure, inspect `.temp/validation/*` logs first.
- Use non-silent fallback (`pnpm run <command-without-silent>`) only when silent output/log is not actionable.

## Definition of Done
- Datetime utilities and types are implemented and exported from SDK public surface.
- All automated tests listed in `## Expected Automated Tests` are implemented.
- Public documentation is updated (`docs` index + dedicated datetime doc + README usage snippet).
- Temporary numbered handoff document is created under project `.temp/` for consumer AI agents.
- Mandatory validation gates complete successfully in required order.
- Lint result is zero warnings and zero errors.
- All tests pass.
- All public API outputs remain camelCase.
- Security requirements from project rules are satisfied.
- Docs are updated for this public contract/usage change.

## Consumer Rollout Note
After SDK release, `miso-ui` and `dataplane app-ui` can replace local datetime formatters with the shared SDK utility to remove format drift and keep locale behavior consistent across products.

## Plan Validation Report

**Date**: 2026-07-24
**Plan**: `/home/dev02/aifabrix-miso-client/.cursor/plans/63.0_locale_datetime_sdk_d32869a3.plan.md`
**Status**: ✅ VALIDATED

### Plan Purpose

- Standardize datetime rendering in consumer UIs via one SDK utility surface.
- Preserve locale-aware and timezone-aware display behavior based on browser/runtime defaults.

### Applicable Rules

- ✅ `/home/dev02/aifabrix-miso-client/.cursor/rules/project-rules.mdc` - TypeScript/public API conventions and camelCase output requirements mapped.
- ✅ `/home/dev02/aifabrix-miso-client/.cursor/rules/project-rules.mdc` - Testing conventions mapped to explicit `Expected Automated Tests`.
- ✅ `/home/dev02/aifabrix-miso-client/.cursor/rules/project-rules.mdc` - Security and feature-completion requirements (types + exports + tests + docs) mapped.

### DoD Gate Readiness

- ✅ Ordered commands are explicit and match required silent gate order.
- ✅ Zero-warning lint requirement is explicit.
- ✅ Tests and documentation requirements are explicit.

### Todo Synchronization

- ✅ Frontmatter `todos` are valid (`id`, `content`, `status`).
- ✅ Todos align with plan sections/phases.
- ✅ Missing phases added (scope/prerequisites, automated tests, ordered validation gates, final closure).

### Updates Applied

- Added required plan structure sections (`Scope`, `Rules and Standards`, `Before Development`, `Expected Automated Tests`, `Validation`).
- Reworked frontmatter todos to map one-to-one with execution phases.
- Hardened Definition of Done with mandatory readiness statements and quality/security guarantees.
- Appended this in-file validation report.

### Risks / Follow-ups

- Locale output can still vary by ICU/runtime; keep assertions resilient and use explicit locale/timezone tests.
- Consumer repos must follow handoff checklist to avoid mixed fallback semantics during migration.

## Strict Execution Checklist (Recommended Order)

Execute todos in this exact order. Start the next item only after the exit criteria of the current item are satisfied.

1. `scope-and-prerequisites`
   - Exit criteria:
   - Contract and non-goals are explicitly confirmed in the implementation notes.
   - Input/fallback semantics are frozen before code edits.

2. `implement-datetime-types-and-utils`
   - Exit criteria:
   - `datetime.types.ts` and `datetime.utils.ts` exist with finalized API signatures.
   - Default behavior is browser/runtime locale + browser local timezone.
   - Utility behavior is no-throw for invalid/missing values.

3. `export-sdk-surface`
   - Exit criteria:
   - New datetime symbols are exported from `src/sdk-exports.ts`.
   - Public package entry remains consistent through `src/index.ts`.

4. `implement-automated-tests`
   - Exit criteria:
   - `tests/unit/datetime.utils.test.ts` covers all scenarios from `## Expected Automated Tests`.
   - Tests assert locale/timezone behavior using explicit overrides where determinism is required.

5. `update-public-docs`
   - Exit criteria:
   - `docs/datetime-formatting.md` is added.
   - `docs/README.md` and `README.md` include datetime usage references.
   - Docs clearly describe fallback behavior and consumer migration guidance.

6. `create-temp-consumer-agent-handoff`
   - Exit criteria:
   - Temporary handoff doc is created under project-root `.temp/` (or subfolder).
   - File uses next sequential numeric prefix in that folder.
   - Handoff includes import examples, replacement steps, and verification checklist for `aifabrix-miso` and `aifabrix-dataplane`.

7. `run-ordered-validation-gates`
   - Exit criteria:
   - All commands in `## Validation` run in the defined order.
   - Any `*:silent` failure is triaged via `.temp/validation/*` first.
   - Lint is zero warnings/errors and tests pass.

8. `final-dod-closure`
   - Exit criteria:
   - Every item in `## Definition of Done` is verifiably satisfied.
   - Plan is ready for implementation handoff with no open blocking gaps.

## Implementation Completion Report

**Date**: 2026-07-24  
**Status**: ✅ COMPLETE

### Delivered
- Added `src/types/datetime.types.ts` with datetime input/options contract.
- Added `src/utils/datetime.utils.ts` with `formatDateTime`, `formatDate`, `formatTime`, `createDateTimeFormatter`.
- Exported datetime surface via `src/sdk-exports.ts`.
- Added `tests/unit/datetime.utils.test.ts` covering locale/timezone/fallback/parity scenarios.
- Added docs page `docs/datetime-formatting.md` and linked it from `docs/README.md` and `README.md`.
- Added consumer-agent handoff under `.temp/agent-handover/01-datetime-sdk-consumer-handoff.md`.

### Validation Gates Executed (in required order)
1. `pnpm run tests:typecheck:silent` - PASS
2. `pnpm run build:silent` - PASS
3. `pnpm run fmt:silent` - PASS
4. `pnpm run md:lint:silent` - PASS
5. `pnpm run lint:silent` - PASS
6. `pnpm run test:silent` - PASS

### DoD Closure
- Datetime utilities and types are implemented and exported from SDK public surface. ✅
- Automated tests for expected scenarios are implemented and passing. ✅
- Public docs and README usage are updated. ✅
- Temporary numbered consumer-agent handoff document is created in project `.temp/`. ✅
- Ordered validation gates completed successfully with zero lint warnings/errors and passing tests. ✅

## Validation Report

**Date**: 2026-07-24
**Status**: ✅ COMPLETE

### Executive Summary

- Plan implementation is complete and evidence-backed across code, tests, docs, and rollout handoff artifact.
- All mandatory quality gates passed in strict required order using silent runners.

### Task Completion

- Total: 8
- Completed: 8
- Incomplete: 0

### Task State Synchronization

- ✅ Markdown checkboxes synchronized
- ✅ Frontmatter `todos` synchronized
- ✅ No contradictions remain

### File and Implementation Validation

- ✅ `src/types/datetime.types.ts` exists and defines datetime input/options contract.
- ✅ `src/utils/datetime.utils.ts` exists and exports `formatDateTime`, `formatDate`, `formatTime`, `createDateTimeFormatter`.
- ✅ `src/sdk-exports.ts` exports datetime types and utils via public SDK surface.
- ✅ `docs/datetime-formatting.md` exists and is linked from `docs/README.md` and `README.md`.
- ✅ `.temp/agent-handover/01-datetime-sdk-consumer-handoff.md` exists with consumer-agent migration guidance.

### Automated Tests Validation

- ✅ Unit tests exist for changed behavior (`tests/unit/datetime.utils.test.ts`)
- ✅ Expected automated tests implemented (locale, timezone override, fallback handling, formatter parity)

### Quality Gates

- ✅ tests:typecheck (`pnpm run tests:typecheck:silent`)
- ✅ build (`pnpm run build:silent`)
- ✅ fmt (`pnpm run fmt:silent`)
- ✅ md:lint (`pnpm run md:lint:silent`)
- ✅ lint (`pnpm run lint:silent`) - 0 warnings, 0 errors
- ✅ test (`pnpm run test:silent`)

### Rules Compliance

- ✅ SDK token/header policy
- ✅ service/redis/error-handling patterns
- ✅ RFC 7807 / security / camelCase API

### Logs

- Full logs: `.temp/validation/*`

### Issues and Recommendations

- No blocking issues found.
- Optional next step: migrate consumer repos (`aifabrix-miso`, `aifabrix-dataplane`) to replace local datetime helpers with SDK utilities using the handoff checklist.

### Final Checklist

- [x] All tasks implemented and synchronized
- [x] Files and tests validated
- [x] Quality gates passed in strict order
- [x] Rules compliance verified